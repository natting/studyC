

void main()
{
  int rp = 0;// read point
  int wp = 0;// write point
  
  int Q[wp++] = 0;//시작점
  
  while (rp < wp) {
    Start = Q[rp++];//Q get
    if (....) {
      Q[wp++] = value;
    }
  }
}
